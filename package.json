{
  "name": "watch-it-think",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Watch a 5M parameter router, trained from random init, resolve a sentence into an intent.",
  "scripts": {
    "check:tokenizer": "tsx tools/check-tokenizer.ts",
    "check": "npm run check:tokenizer",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^24.13.6",
    "tsx": "4.20.6",
    "typescript": "5.9.3"
  }
}
